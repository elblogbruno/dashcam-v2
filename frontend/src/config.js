// API base URL configuration
// This can be changed depending on environment (development, production, etc.)

export const API_BASE_URL = '/api';

// Other global configuration options can be added here
export const DEFAULT_MAP_CENTER = [34.0522, -118.2437]; // Default map center (Los Angeles)
export const DEFAULT_MAP_ZOOM = 10; // Default map zoom level